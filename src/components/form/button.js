import React from 'react';

const Button = () => {

    return (
        <>
            <div>
                <button className="main-btn" type="submit">Add to list</button>
            </div>
        </>
    );
}

export default Button;