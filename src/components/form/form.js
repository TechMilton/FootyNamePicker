import React, { useState, useReducer } from "react";
import initialState from '../../data/initialState';

const Form = () => {









    return (
        <h2>Hello, World! - From the Form component</h2>
    );

}

export default Form;